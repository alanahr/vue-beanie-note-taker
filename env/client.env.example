# Client (Vue front-end) environment configuration
# Copy this file to env/client.env and adjust values as needed

# Port the Vite preview server listens on inside the container
CLIENT_PORT=5173

# Base URL the front-end uses to reach the back-end API
VITE_API_BASE_URL=http://localhost:8002
