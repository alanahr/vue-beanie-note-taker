# Test-specific environment configuration
# Copy this file to env/test.env and adjust values as needed

# URL Cypress uses to reach the front-end inside the Docker network
CYPRESS_BASE_URL=http://client:5173
